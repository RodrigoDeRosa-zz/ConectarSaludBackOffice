import { RequestPrescriptionAndConsultationDto } from './request-prescription-and-consultation-dto';

describe('RequestPrescriptionAndConsultationDto', () => {
  it('should create an instance', () => {
    expect(new RequestPrescriptionAndConsultationDto()).toBeTruthy();
  });
});
